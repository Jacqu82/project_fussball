{% extends 'base.html.twig' %}

{% block body %}
    <div class="container text-center">
        <h2>Edycja zawodnika</h2>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form(form) }}
        {{ form_end(form) }}
    </div>
    <hr/>
    <p class="text-center">
        <a href="{{ path('player_show_id', {'id': player.id}) }}" class="btn btn-default">Powrót</a>
    </p>
{% endblock %}