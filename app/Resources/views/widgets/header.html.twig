{% extends "::base.html.twig" %}

{% block body %}
    {% if not app.user %}
        <div class="container">
            <div class="navbar-header">
                <div class="container text-center">
                    <div class="navbar-header">
                        <a class="navbar-brand" href="{{ path('homepage') }}">Strona główna</a>
                    </div>
                    <div class="collapse navbar-collapse" id="myNavbar">
                        <ul class="nav navbar-nav">
                            <li><a href="{{ path('show_all') }}">Kluby ekstraklasy</a></li>
                        </ul>
                        <ul class="nav navbar-nav navbar-right">
                            <li>
                                <a href="{{ path('fos_user_registration_register') }}">Utwórz konto</a>
                            </li>
                            <li><a href="{{ path('fos_user_security_login') }}">
                                    <span class="glyphicon glyphicon-log-in"></span> Zaloguj się
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    {% else %}
        {% set user = app.user %}
        <div class="container">
            <div class="navbar-header">
                <div class="container text-center">
                    <div class="navbar-header">
                        <a class="navbar-brand" href="{{ path('homepage') }}">Strona główna</a>
                    </div>
                    <div class="collapse navbar-collapse" id="myNavbar">
                        <ul class="nav navbar-nav">
                            {#<li><a href="{{ path('show_users') }}">Admin Panel</a></li>#}
                            {#<li><a href="{{ path('sonata_admin_dashboard') }}">{{ 'label.sonata_page'|trans }}</a></li>#}
                            <li><a href="{{ path('show_all') }}">Kluby ekstraklasy</a></li>
                        </ul>
                        <ul class="nav navbar-nav navbar-right">
                            <li class="user" style="margin-top: 15px;">Zalogowany jako
                                <span style="color: gold">{{ app.user.username }}</span>
                            </li>
                            <li><a href="{{ path('fos_user_security_logout') }}">
                                    <span class="glyphicon glyphicon-log-out"></span> Wyloguj
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}