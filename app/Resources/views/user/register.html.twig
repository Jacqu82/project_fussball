{% extends 'base.html.twig' %}

{% block body %}
    <div class="container text-center">
        <h1>Utwórz konto</h1>
        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form(form) }}
        {#<div class="g-recaptcha" data-sitekey="6Ld5ujsUAAAAAOdfA6s7YBwAQYD0vJXAO3iV900r"></div>#}
        {{ form_row(form.submit) }}
        {{ form_end(form) }}
        <h3>Masz konto?</h3>
        <h4><a href="{{ path('login') }}" class="links btn btn-info">Zaloguj się</a></h4>
    </div>
{% endblock %}
