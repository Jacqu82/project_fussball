{% extends 'base.html.twig' %}

{% block body %}
    <div class="container text-center">
        <h2>Edycja klubu</h2>
        {{ form_start(form) }}
        {{ form(form) }}
        {{ form_end(form) }}
    </div>
    <hr/>
    <p class="text-center">
        <a href="{{ path('club_show_id', {'id': club.id}) }}" class="btn btn-info">Powrót</a>
    </p>
{% endblock %}