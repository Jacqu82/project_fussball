{% extends "::base.html.twig" %}

{% block body %}
    {% include 'widgets/header.html.twig' %}
    <div class="container text-center">
        <h3>Aktualne zdjęcie:</h3>
        <img src='{{ asset( club.getWebPath ) }}' width="300" height="300">
        <hr/>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        <hr/>
        <a href="{{ path('show_id', { 'id': club.id }) }}" class="btn btn-default">Powrót</a>
    </div>
    {% include 'widgets/footer.html.twig' %}
{% endblock %}