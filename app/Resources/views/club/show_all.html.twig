{% extends "::base.html.twig" %}

{% block body %}
    <div class="container text-center">
        <h1>Kluby Ekstraklasy</h1>
        <hr/>
        <h3>Mój klub</h3>
        {% for club in clubs %}
            {% if club.name == 'Górnik Zabrze' %}
                <a href="{{ path('show_id', {'id': club.id}) }}" class="links btn btn-danger">{{ club.name }}<br/>
                    <img src="{{ club.getWebPath }}" width="200" height="200">
                </a>
            {% endif %}
        {% endfor %}
        <h3>Pozostałe kluby</h3>
        {% for club in clubs %}
            {% if club.name != 'Górnik Zabrze' %}
                <a href="{{ path('show_id', {'id': club.id}) }}" class="links btn btn-primary">{{ club.name }}<br/>
                    <img src="{{ club.getWebPath }}" width="200" height="200">
                </a>
            {% endif %}
            {#{% else %}#}
            {#<a href="{{ path('show_id', {'id': club.id}) }}" class="links btn btn-primary">{{ club.name }}<br/>#}
            {#<img src="{{ club.getWebPath }}" width="200" height="200">#}
            {#</a>#}
            {#{% endif %}#}
            {#<a href="{{ path('show_id', {'id': club.id}) }}" class="links btn btn-primary">{{ club.name }}<br/>#}
            {#<img src="{{ club.getWebPath }}" width="200" height="200">#}
            {#</a>#}
        {% endfor %}
        <hr/>
        <a href="{{ path('homepage') }}" class="btn btn-default">Strona główna</a>
    </div>
{% endblock %}